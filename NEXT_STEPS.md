# Следващи стъпки (обновено 2026-09-24)

> **Тази бележка е, за да се пестят токени, не за да се харчат повече.**
> Тук вече е записано какво е проверено и решено. Вярвай ѝ и започни директно
> от задачата. Не преглеждай наново цялото репо и не повтаряй анализа.
> Провери с grep само файловете, които ще пипаш. Отговаряй кратко. Когато
> нещо се свърши, обнови бележката накратко: зачеркни или изтрий готовото,
> не трупай нов текст.

## Решения на оператора

- **Само терминал.** Гласът, зрението и 24/7 режимът са махнати (PR #2).
- **Бекъп само ръчно.** `genesis fix` прави снимка само с `--checkpoint`;
  в чата — `/backup`. Нищо не се архивира автоматично (PR #3).
- **Бърз старт, нищо не се помни.** Стартовият модел е groq `openai/gpt-oss-120b`
  (най-бързият с инструменти наживо: 0.4–1.5 s). Последният модел НЕ се пази
  между сесиите — `last_model.json` е махнат изцяло (PR #5).
- **Малките модели за леките неща, силните за тежките** (PR #3, #4):
  фонова работа (памет, резюмета, граф) → `light_models`; в чата явно лек
  въпрос → малък модел без инструменти, иначе силният. Поиска ли малкият
  инструмент — ходът отива на силния. `GENESIS_CHAT_ROUTING=0` изключва.

## Направено на 2026-09-23 (PR #1–#5, всички слети)

| | Какво | Измерено |
|---|---|---|
| #1 | големият ъпгрейд (сигурност, Gemini/Vertex, `/update`, 21 умения) | CI зелено |
| #2 | махнати GUI/глас/зрение/24-7; браузърни схеми не се пращат без playwright; датата в промпта; `/skills`; промптът иска независимите инструменти в един рунд | −1 650 знака/обръщение |
| #3 | бекъп по заявка; `max_tokens:` по модел; 410 → пропуск за 24h; `Brain(light=True)`; махнати 4 мъртви модела | лека задача 0.6–3 s срещу 8–45 s |
| #4 | маршрутизация в чата (лек въпрос → малък модел) | 0.5–1.4 s, ~2 300 токена срещу ~5 500 |
| #5 | бърз стартов модел, без помнене на последния модел | — |

## Модели — измерено 2026-09-23 (PR #6)

`scripts/bench_models.py --tasks 21` (трудните задачи, кодът се проверява с
тестове) + първи ход с инструменти (5 задачи, истинският промпт на чата):

| Модел | Код 21 | с/задача | Инструменти 5 | Роля |
|---|---|---|---|---|
| groq gpt-oss-120b | 95% | ~1.2 | 4/5, 0.9 s | **старт** |
| ollama gpt-oss-120b | 95% | ~2.1 | 3/5, 1.3 s | резерва 1 |
| nvidia nemotron-ultra | 100%* | ~14 | — | резерва 2 |
| ollama nemotron-ultra | — | — | 4/5, 7.7 s | по-надолу (бавен) |
| groq gpt-oss-20b | 100% | ~1.1 | — | лек №1 |
| ollama gpt-oss-20b | 100% | ~10 | — | лек №2 |
| nvidia nemotron-super | 100%* | — | — | лек №3 |

\* NVIDIA реже с лимит след ~8–18 поредни заявки (оттам „няма код"); оценката е
само за отговорите, които стигнаха. Groq: лимит на токени/минута — ~5 пълни
заявки изчерпват ключ, после веригата продължава (~0.2 s на опит).
Бъг, намерен от бенчмарка: 503 „overloaded" спираше ЦЕЛИЯ ключ → поправено,
503 спира само модела (`_KEY_DEAD_CODES`).

## `genesis fix` — измерено 2026-09-24 (PR #9, `scripts/bench_fix.py`)

12 проекта с един истински бъг и падащ тест; успехът се проверява повторно
и тестът не бива да е пипан. Конфигурираната верига:

| | Преди | След |
|---|---|---|
| Поправени | 12/12 | 12/12 |
| Средно време на задача | 1 142 s | **12.1 s** |
| Токени на задача | 11 376 | **6 520** (−43%) |
| Рундове | 6 от 6 навсякъде | 2–5 |
| Фалшиво „поправено" | 0 | 0 |

Какво го направи: (1) тестовете се пускат веднага след успешна редакция —
зелено → край, червено → изходът отива при модела наготово; (2) на Windows
`WindowsApps` отзад в PATH на командите — голото `python` беше заглушката от
Microsoft Store, която виси до таймаута (~400 s на команда); (3) таймаутът
убива цялото дърво процеси (`taskkill /T`) и не чака изхода вечно — преди
висеше 11 246 s при таймаут от 120; (4) `C:\...\python.exe` без кавички в
началото на командата → `/`, иначе bash връща rc=127.

## Измерено (budget_log.jsonl, живи проби)

- 93% от токените са промпт. Основата на обръщение към силния модел:
  системен промпт ~1 900 токена, схеми на инструменти ~1 600, среда ~140.
- Български текст струва ~1.7× повече токени от същия смисъл на английски
  (36 срещу 21 при един и същ текст).
- Безплатните каталози гният бързо: за 6 седмици 4 от ~17 модела умряха
  (410/404). Мъртъв №2 във веригата изглеждаше като „NVIDIA пада 40%".
- Кеш: 0 навсякъде — кеширането е само за Anthropic.

## Следва — по реда на ефекта

1. ~~`genesis models --check`~~ — PR #7. Мъртвите се прескачат сами, чатът
   проверява веднъж седмично на заден план.
2. ~~По-къси схеми~~ — PR #7: 1 414 → 1 005 токена (−29%). TASK_* не са
   слети — сменя и кода зад тях, печалбата е малка.
3. ~~По-къс системен промпт~~ — PR #7: 1 786 → 1 028 токена (−42%). Основа
   общо 3 345 → 2 178 (−35%). Проверено: ollama gpt-oss-120b 10/15 с новия
   срещу 8/15 със стария (5 задачи × 3), groq 120b 4/5 и преди, и след.
4. ~~Машинният текст на английски~~ — ИЗМЕРЕНО и отхвърлено: само 7% от
   резултатите на инструментите е кирилица (останалото е съдържание) →
   под 5% печалба срещу десетки съобщения и тестове за пипане.
5. **Мерене на групирането на инструменти** — след ден-два работа
   `genesis budget`: обръщения на задача трябва да паднат от ~18.
6. ~~Команда вместо модел~~ — PR #13: „направи бекъп“ → предлага `/backup`
   (+ `/skills`, `/models`, `/update`); ≥2 178 токена → 0, ~2 µs.
7. ~~Бенчмарк за `genesis fix`~~ — `scripts/bench_fix.py`, 12 проекта (PR #9).
   Повторно пускане след всяка промяна по `repair()` или веригата.
8. ~~Уменията като процесите на оператора~~ — операторът: не трябват (2026-09-24).
9. ~~Forge-ът за скилове~~ — махнат (PR #11): `/autoupgrade`, `parallel_forge`,
   `goal_engine`, `product_api`. Остават `orchestrator`/`verifier` (ползва ги
   `bench_models.py`) и `notifier` (`genesis mission`).
10. ~~`mistral-nemotron` (NVIDIA)~~ — махнат (PR #10): таймаут 30 s два дни.
11. ~~Мъртъв код~~ — PR #14: `ensemble`, `project_builder`, `skills_api` (−495 реда).
12. ~~Бавен Windows CI~~ — PR #15: 14 мин → 2.5 мин (тестовете чукаха ollama).
13. ~~Учебните умения~~ — ИЗМЕРЕНО и отхвърлено: 6 от 23 попадения за 15 реални
    заявки, почти всички по темата; за работните заявки не излизат.
14. **`bge-m3` вместо `nomic-embed-text`** — на лаптопа, с ollama (виж капаните).
15. ~~`/backup` на Windows~~ — PR #17: без rsync копира с Python; цел в работната
    папка се отказва (преди `[WinError 2]`).
16. ~~Още фрази към команди~~ — PR #18: 4 → 8 команди; само `/backup`, `/clear` питат.
17. ~~Мъртви функции~~ — PR #19: vulture + grep, −42 реда.
18. ~~Бърз старт~~ — ИЗМЕРЕНО: 0.3 s до подканата, внасяне 0.31 s — няма какво да се гони.
19. ~~Шум в търсенето на умения~~ — ИЗМЕРЕНО и отхвърлено: ползва се само в
    `genesis mission` и бенчмарка, не в чата; пълен код само при ≥2 думи;
    случайно попадение = 1 ред (~25 токена) в „Други подобни".

**Нов план (2026-09-24) — всичко останало иска лаптопа (ключове, ollama):**

1. Преинсталирай от `main`, после мери отново: `python scripts/bench_fix.py`
   (очаквано 12/12, ~12 s) и `genesis models --check` (веригата без nemotron).
2. Точка 14: `ollama pull bge-m3`, мерене на търсенето на умения на български
   преди/след.
3. Точка 5: след ден-два работа — `genesis budget`, обръщения на задача (~18 преди).

**Да не се прави:** нови интерфейси и нови модули. Качеството идва от измерване
и изглаждане на основния цикъл.

## Капани, открити по пътя

- Тестовете на Windows през споделена конзола получават случаен
  `KeyboardInterrupt`; пускай ги в отделна: `Start-Process ... -WindowStyle Hidden`.
- Тест, който стига до ollama или истински модел, на Windows чака ~2 s на
  опит (или 8 s в `Brain.complete`). `conftest.py` отказва порт 11434;
  CI печата `--durations=15` — бавен тест се вижда там.
- Жива проба през нормален `Brain()` пише в `budget_log.jsonl` — това е
  истински разход и е правилно да се брои.
- `nomic-embed-text` е предимно английски — да се пробва `bge-m3` заради
  българския („изчисти/изчистя").
