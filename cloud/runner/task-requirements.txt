# Библиотеки, готови в образа: задачите нямат мрежа към PyPI (само към
# моделите), затова най-честите нужди са вътре. Всяка добавка тук = по-голям
# образ; мери с `docker images` преди и след.
openpyxl      # Excel
pandas        # таблици, CSV
pypdf         # четене на PDF
python-docx   # Word
pillow        # снимки
beautifulsoup4  # HTML
pytest        # тестове на проекти
